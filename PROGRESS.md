# Progress

2026-09-02 baseline: repository `G:\TencentDB-Agent-Memory`, branch `feat/server_team`; CodeGraph 776 files / 14,413 nodes / 49,638 edges.
Goal: deliver project-scoped team-memory plugin, Web governance surface, and explicit deterministic fixture without implementing the real backend.
Order: inspect existing plugin/Web patterns; add fixture and typed API client; wire plugin capture/recall/lifecycle; add Web pages and role-aware controls; run focused tests and reverse-failure checks.
Maximum risk: existing baseline test/format/tooling blockers and keeping all client state server-authoritative while preserving the whitelist.
Dependency installs: MemoryPanel `pnpm install` passed; MemoryPanel/web `npm ci` passed.
Baseline: MemoryPanel `pnpm typecheck` passed; `pnpm test` blocked by no test files; Web build passed; lint passed with 32 warnings; format blocked by 147 files; MemoryCore test blocked by missing vitest.

2026-09-03 completion checks:

- README/config alignment: removed obsolete `capture.enabled`; documented auto-capture as always on in both OpenClaw READMEs and the adapter architecture note.
- HTTP fixture smoke: recall outage `503 MEMORY_SERVICE_UNAVAILABLE`; cross-project read `403 PROJECT_ACCESS_DENIED`; stale update `409 MEMORY_REVISION_CONFLICT`; cleanup-failed delete `202` followed by recall `200 READY` with zero items. Plugin client/hook returned `UNAVAILABLE` without throwing and preserved server error code details. Replayed capture returned the same `202`/job receipt.
- `pnpm --dir G:\TencentDB-Agent-Memory\MemoryPanel typecheck`: exit 0.
- `pnpm --dir G:\TencentDB-Agent-Memory\MemoryPanel test`: exit 0; 5 files, 26 tests passed, skip/todo 0.
- `npm --prefix G:\TencentDB-Agent-Memory\MemoryPanel\web run build`: exit 0; 1352 modules transformed.
- `npm --prefix G:\TencentDB-Agent-Memory\MemoryPanel\web run lint:check`: exit 0; 0 errors, 32 existing warnings.
- `npm --prefix G:\TencentDB-Agent-Memory\MemoryPanel\web run format:check`: exit 1; 147 existing files reported (baseline formatting debt; newly added files were checked independently).
- `npm --prefix G:\TencentDB-Agent-Memory\MemoryCore\openclaw-plugin run build`: exit 0.
- `npm --prefix G:\TencentDB-Agent-Memory\MemoryCore\pi-plugin run build`: first exit 1 because declared dependencies were absent; after `npm install --no-save --no-package-lock --no-audit --no-fund`, exit 0. Direct `tsc --noEmit --pretty false`: exit 0.
- `npm --prefix G:\TencentDB-Agent-Memory\MemoryCore test`: exit 1; `vitest` not recognized (existing dependency blocker).
- `git diff --check`: exit 0. Existing `bash MemoryPanel/scripts/secret-scan.sh` command failed before scanning because the script has CRLF under the available bash; recorded in `BLOCKED.md`.
- Post-fix regression: `MemoryPanel` test suite exit 0 with 5 files / 31 tests passed / skip-todo 0; client-level tests prove capture idempotency and capture/update/recall/delete synchronization, and hook tests preserve server error codes. Web list responses reject older request generations after project or filter changes. Plugin, panel, and new documentation paths contain no obsolete `capture.enabled` references; existing out-of-scope `MemoryCore/index.ts` and installer references remain unchanged.
- Final source-tree hygiene: removed six compiler-generated `.js` files from `MemoryCore/openclaw-plugin/src/**` and `MemoryCore/pi-plugin/**`; no generated source files remain. `git diff --check` and whitelist checks were rerun after cleanup.
- Current-turn verification: `MemoryPanel` typecheck and full test suite passed again (5 files / 31 tests / skip-todo 0); Web build passed with 1,352 transformed modules and lint passed with 0 errors / 32 existing warnings; OpenClaw and Pi `npx tsc --noEmit --pretty false` both exited 0. Focused reverse fixture run passed 3 tests covering unavailable `503`, forbidden/revision `403`/`409`, and cleanup-failed delete filtering. Final checks report `WHITELIST_OK`, `NO_OBSOLETE_CAPTURE_ENABLED_REFERENCES_IN_SCOPE`, `NO_GENERATED_SOURCE_JS`, and `git diff --check` exit 0.
- User-authorized gate closure: full Web Prettier formatting was run and `format:check` now passes; MemoryCore dependencies were installed with `npm install --ignore-scripts --no-package-lock --no-audit --no-fund` because the existing Windows-incompatible postinstall uses `bash ... || true`; `secret-scan.sh` was normalized from CRLF to LF and the scan now passes.
- Post-authorization verification: Web build passed (1,352 modules), lint passed (0 errors / 33 existing warnings), Panel typecheck and tests passed (5 files / 31 tests / skip-todo 0), and OpenClaw/Pi builds passed. MemoryCore `npm test` now starts Vitest 4.1.11 but exits 1 because the repository has no files matching `src/**/*.test.ts` or `__tests__/**/*.test.ts`; no test files were added because the task whitelist excludes `MemoryCore` root tests.
- Plugin build regenerated six source-tree `.js` artifacts; they were removed again after verification. Do not rerun those build scripts without an isolated output directory.
- Final authorized gate results: Web `npm run format:check` exits 0 after the one requested full-format pass; `bash MemoryPanel/scripts/secret-scan.sh` exits 0 with no sensitive information; MemoryCore dependencies are present and both plugin builds exit 0. The secret-scan file is physically LF-only, no MemoryCore lockfile was created, and final source-artifact/whitelist checks remain clean.
- Whitelist expansion: added `MemoryCore/src/core/memory-generation-log/store.test.ts` with 3 real tests for built-in and resolved prompt provenance plus generation-key partitioning/safe-ID handling. `npm test` now exits 0 with 1 file / 3 tests passed / skip-todo 0; the focused test command and Prettier check also exit 0. `NO_GENERATED_SOURCE_JS`, `MEMORYCORE_ROOT_TEST_WHITELIST_OK`, and `git diff --check` pass.
- Documentation audit, 2026-09-03: updated the plugin, Web admin, and service API memory-library documents to distinguish delivered OpenClaw/Pi/Web/fixture work from target behavior and real-backend requirements. The documents now state that `#/project-memory` is one four-tab page; plugin member UI and Pi turn hooks are not delivered; fixture-only behavior is not production-service evidence.
